apiVersion: v1
kind: PersistentVolume
metadata:
  name: prometheus-pv-data-volume
  namespace: lens-metrics
  labels:
    type: local
spec:
  storageClassName: local-storage
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: /media/bigdata/storage/configs/prometheus/
    server: 192.168.1.60
